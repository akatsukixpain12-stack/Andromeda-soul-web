# Andromeda Inference Engine

- **KV Cache**: Key-Value cache for $O(1)$ token generation per step.
- **Logit Sampler**: Supports Temperature, Top-K, Top-P (Nucleus), and Repetition Penalty.
- **Streaming**: Server-Sent Events (SSE) token stream generator.
